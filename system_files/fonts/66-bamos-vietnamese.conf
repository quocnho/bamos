<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<!--
  BamOS Vietnamese Font Configuration
  Priority order for font fallback chains
  Includes Microsoft symbol/dingbat font replacements
-->
<fontconfig>

  <!-- ================================================================ -->
  <!-- Default Font Families for Vietnamese                             -->
  <!-- ================================================================ -->

  <alias>
    <family>serif</family>
    <prefer>
      <family>Noto Serif</family>
      <family>Liberation Serif</family>
      <family>DejaVu Serif</family>
    </prefer>
  </alias>

  <alias>
    <family>sans-serif</family>
    <prefer>
      <family>Noto Sans</family>
      <family>Inter</family>
      <family>Liberation Sans</family>
      <family>DejaVu Sans</family>
    </prefer>
  </alias>

  <alias>
    <family>monospace</family>
    <prefer>
      <family>Maple Mono NF</family>
      <family>Noto Sans Mono</family>
      <family>Liberation Mono</family>
      <family>DejaVu Sans Mono</family>
    </prefer>
  </alias>

  <!-- ================================================================ -->
  <!-- Microsoft Office Font Replacements (Metric-Compatible)           -->
  <!-- ================================================================ -->

  <alias binding="same">
    <family>Times New Roman</family>
    <accept><family>Liberation Serif</family></accept>
  </alias>

  <alias binding="same">
    <family>Arial</family>
    <accept><family>Liberation Sans</family></accept>
  </alias>

  <alias binding="same">
    <family>Arial Narrow</family>
    <accept><family>Liberation Sans Narrow</family></accept>
  </alias>

  <alias binding="same">
    <family>Courier New</family>
    <accept><family>Liberation Mono</family></accept>
  </alias>

  <alias binding="same">
    <family>Tahoma</family>
    <accept><family>Liberation Sans</family></accept>
  </alias>

  <alias binding="same">
    <family>Calibri</family>
    <accept><family>Carlito</family></accept>
  </alias>

  <alias binding="same">
    <family>Cambria</family>
    <accept><family>Caladea</family></accept>
  </alias>

  <!-- ================================================================ -->
  <!-- Microsoft Symbol/Dingbat Font Replacements                       -->
  <!-- Required to prevent WPS Office / LibreOffice from showing        -->
  <!-- "Some formula symbols might not be displayed correctly" errors   -->
  <!-- ================================================================ -->

  <!-- Symbol → OpenSymbol (from libreoffice-opensymbol-fonts) -->
  <alias binding="same">
    <family>Symbol</family>
    <accept>
      <family>OpenSymbol</family>
      <family>DejaVu Sans</family>
    </accept>
  </alias>

  <!-- Wingdings → DejaVu Sans (includes many dingbat/symbol glyphs) -->
  <alias binding="same">
    <family>Wingdings</family>
    <accept>
      <family>DejaVu Sans</family>
    </accept>
  </alias>

  <alias binding="same">
    <family>Wingdings 2</family>
    <accept>
      <family>DejaVu Sans</family>
    </accept>
  </alias>

  <alias binding="same">
    <family>Wingdings 3</family>
    <accept>
      <family>DejaVu Sans</family>
    </accept>
  </alias>

  <!-- Webdings → DejaVu Sans -->
  <alias binding="same">
    <family>Webdings</family>
    <accept>
      <family>DejaVu Sans</family>
    </accept>
  </alias>

  <!-- MT Extra → DejaVu Sans (mathematical symbols) -->
  <alias binding="same">
    <family>MT Extra</family>
    <accept>
      <family>DejaVu Sans</family>
    </accept>
  </alias>

  <!-- ================================================================ -->
  <!-- Vietnamese Proprietary Font Aliases (when installed by user)     -->
  <!-- These do nothing unless the actual font files exist              -->
  <!-- ================================================================ -->

  <alias>
    <family>VNI-Times</family>
    <accept><family>Noto Serif</family></accept>
  </alias>

  <alias>
    <family>VNI-Helve</family>
    <accept><family>Noto Sans</family></accept>
  </alias>

  <alias>
    <family>VNI-Aptima</family>
    <accept><family>Inter</family></accept>
  </alias>

  <alias>
    <family>.VnTime</family>
    <accept><family>Noto Serif</family></accept>
  </alias>

  <alias>
    <family>.VnArial</family>
    <accept><family>Noto Sans</family></accept>
  </alias>

  <alias>
    <family>.VnTimeH</family>
    <accept><family>Noto Serif</family></accept>
  </alias>

  <!-- ================================================================ -->
  <!-- Rendering Settings                                               -->
  <!-- ================================================================ -->

  <!-- Sub-pixel rendering for LCD screens -->
  <match target="font">
    <edit name="rgba" mode="assign"><const>rgb</const></edit>
  </match>

  <!-- Slight hinting — best balance for Latin + Vietnamese diacritics -->
  <match target="font">
    <edit name="hinting" mode="assign"><bool>true</bool></edit>
    <edit name="hintstyle" mode="assign"><const>hintslight</const></edit>
  </match>

  <!-- Anti-aliasing -->
  <match target="font">
    <edit name="antialias" mode="assign"><bool>true</bool></edit>
  </match>

  <!-- Consistent DPI across all toolkits (GTK/Qt/XWayland) -->
  <!-- Prevents "System DPI asymmetric" warnings -->
  <match target="pattern">
    <edit name="dpi" mode="assign"><double>96</double></edit>
  </match>

  <!-- Exclude CJK fonts from Noto Sans fallback (use dedicated CJK family) -->
  <match target="scan">
    <test name="family" compare="eq">
      <string>Noto Sans CJK KR</string>
    </test>
    <edit name="lang" mode="assign">
      <minus><name>lang</name><langset><string>vi</string></langset></minus>
    </edit>
  </match>

</fontconfig>
