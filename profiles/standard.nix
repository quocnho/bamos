# Profile STANDARD — Máy tiêu chuẩn (văn phòng, gia đình, học tập)
# Kế thừa profile desktop (GNOME + macOS WhiteSur + bộ gõ tiếng Việt + audio)
{ config, lib, ... }:

{
  imports = [ ./desktop.nix ];
}
