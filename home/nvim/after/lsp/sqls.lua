-- SQL Language Server (sqls) cho MySQL / MariaDB / PostgreSQL / SQLite
return {
  settings = {
    sqls = {
      connections = {
        -- Kết nối mặc định khi trong devenv có mysql/postgres
      },
    },
  },
}
